[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast unit tests (no I/O)
    eval: evaluation tests that print metric tables (slower)
addopts = -v
